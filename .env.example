# Optional: JSON array for ICE servers (STUN/TURN). If empty, defaults to public STUN only.
# Example adding TURN (replace with your own TURN host/creds):
# ICE_SERVERS_JSON=[{"urls":["stun:stun.l.google.com:19302"]},{"urls":"turn:YOUR_TURN_HOST:3478","username":"user","credential":"pass"}]
ICE_SERVERS_JSON=
PORT=3000
